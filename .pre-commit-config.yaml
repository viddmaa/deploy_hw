repos:
  - repo: local
    hooks:
      - id: pip-audit
        name: pip-audit
        entry: pip-audit -r requirements.txt
        language: system
        pass_filenames: false
        files: requirements.txt